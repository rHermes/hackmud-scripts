function (c, a) {
	// Valid locks:
	// - ez_21
	// - ez_35
	// - ez_40
	// - c001
	// - c002
	// - c003
	// - w4rn
	
	// var clr = ctx.caller;
	var l = #s.scripts.lib();

	var g = /EZ_21/;
	var lol = a.call();
	var lol2 = g.exec(lol);

	// First find first lock.
	// we just assume that this is a 21 or 35 lock.
	if (lol2)
		// THis is EZ_35
	} 

	return { ok:false };
}
